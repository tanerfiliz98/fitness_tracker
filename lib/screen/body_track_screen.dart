import 'package:flutter/material.dart';

class BodyTrackScreen extends StatelessWidget {
  const BodyTrackScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text("Yapım Aşamasında"),
        )
      ],
    );
  }
}